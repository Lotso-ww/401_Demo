# TLS401 RFID + CCD 联动演示项目规格说明

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档状态 | 首版开发基线 |
| 项目仓库 | `401_-demo` |
| 目标 | 将现有 RFID 与 CCD demo 合并为一个应用，完成芯片识别、实时预览、拍照、图片归档和历史播放的页面联动 |
| 需求依据 | 《TLS401上位机产品V1.1送检版本-PRD》前部的首页、皿详情和孔详情界面 |
| 参考实现 | RFID demo：`QT_project/hf_tagaccess`；CCD demo：`CCD_D` |

本文档只定义当前 RFID + CCD 联动 demo，不等同于完整 TLS401 上位机产品规格。

## 2. 项目目标

本项目需要在一个桌面应用中打通以下业务闭环：

```text
选择 1-4 号舱室
  -> 读取并解析 RFID 芯片
  -> 将标签信息绑定到所选舱室
  -> 启动 CCD 实时预览
  -> 按培养孔拍照并保存
  -> 在 16 孔页面查看本轮结果和历史轮次
  -> 在单孔页面播放该孔的历史图像
```

首版重点是设备功能整合和页面间数据联动。UI 使用可替换主题层，后续补充 QSS、字体、图标和图片资源时，不修改设备、存储和业务逻辑。

## 3. 范围

### 3.1 首版范围

- 一个应用同时集成 RFID 读写器和 IDS CCD 相机。
- 首页展示 4 个舱室卡片，并允许用户手动选择当前操作舱室。
- 对当前舱室执行 RFID 标签识别和业务数据解析。
- 将 RFID 标签、舱室和本地补充信息建立持久化关联。
- CCD 连接、断开、实时预览、单帧抓拍、曝光和增益设置。
- 16 孔顺序采集，抓拍成功后自动前进到下一个未完成孔。
- 16 孔网格状态展示、任意孔选择、重拍和替换。
- 图片按标签、轮次、孔位保存，SQLite 保存业务元数据。
- 皿详情展示 16 孔最新图像，并按采集轮次播放历史图像。
- 孔详情展示单孔最新图像，并按时间播放该孔历史图像。
- 设备失败、标签异常、保存失败等错误提示和可恢复操作。

### 3.2 明确不在首版范围

- 舱门传感器和自动开关舱逻辑。
- 四舱自动识别队列及 `4 -> 3 -> 2 -> 1` 自动拍照调度。
- X/Y/Z/L 运动平台控制。
- 自动移动并定位 16 个培养孔。
- 真实多焦平层拍摄和 Z 轴切层。
- 位置校准页面。
- 温度、气体、压力、报警、授权、网络同步、历史环境曲线和工程师设置。
- AI 图片裁切和 AI 位置校准。
- 修改 RFID 标签数据协议。
- 完整送检产品所需的权限、审计和整机异常恢复体系。

## 4. 已确认的产品决策

### 4.1 舱室操作方式

首版只有一套 RFID 读写器和一台 CCD。用户先在首页选择 1-4 号舱室，后续识别、预览和采集操作均作用于当前舱室。应用不推断真实舱门位置，也不自动切换舱室。

### 4.2 16 孔采集方式

没有运动平台时，软件无法让相机自动移动到下一个孔位。因此首版采用“软件自动推进、人工完成物理摆位”的半自动方式：

1. 新建采集轮次后，默认选中第一个未完成孔。
2. 操作员在实时画面中确认当前孔已经位于正确视野。
3. 点击一次拍照按钮。
4. 保存成功后，当前孔标记为完成，软件自动选中下一个未完成孔。
5. 操作员移动培养皿或调整视野后继续拍照。
6. 任何时候都可以点击 16 孔网格中的孔位进行跳转、补拍或重拍。

软件不得在相机视野没有发生变化时自动连续生成 16 张图片，因为这些图片不代表 16 个不同培养孔。

### 4.3 焦平层

真实焦平层依赖 Z 轴运动或相机对焦硬件。当前两个 demo 均不具备该能力，因此首版孔详情为单层图像：

- 数据模型保留 `focal_layer` 字段，首版固定为 `0`。
- 界面显示“单层”，不显示可操作的 7 层控件。
- 不通过复制同一张图片伪造 7 个焦平层。
- 后续接入 Z 轴后，可以在不迁移历史数据的情况下扩展为多层拍摄。

### 4.4 标签外业务信息

现有 RFID 协议保持不变。数据来源规则如下：

| 页面字段 | 数据来源 | 规则 |
| --- | --- | --- |
| 舱室号 | 用户当前选择 | 取值 1-4，不写回标签 |
| 标签 UID | RFID 盘点结果 | 作为本地关联主键之一 |
| 培养皿序号 | RFID `dishNumber` | 标签权威值 |
| 授精时间 | RFID `inseminationTime` | 标签权威值 |
| 女方姓名 | RFID `femaleName` | 标签权威值 |
| 病历号 | RFID `medicalRecordNumber` | 标签权威值 |
| 男方姓名 | 本地补录 | 按标签 UID 保存，允许为空 |
| 发育天数 | 软件计算 | 当前时间与授精时间的整日差，仅作 demo 展示 |
| 入舱/识别时间 | 本地记录 | 本次 RFID 成功绑定舱室的时间 |

标签 UID 是技术关联键，不直接宣称为业务“培养皿 ID”。页面需要显示业务编号时使用“培养皿序号”；需要排障时单独显示“标签 UID”。后续如果后台提供正式培养皿 ID，再增加独立字段。

同一 UID 再次识别时自动加载本地补充信息。标签字段与本地旧缓存不一致时，以本次标签读取结果更新缓存；男方姓名等本地字段不受影响。

## 5. 技术标准

### 5.1 统一开发环境

| 项目 | 统一标准 |
| --- | --- |
| 操作系统 | Windows 10/11 64 位 |
| Qt | Qt 5.14.2，`msvc2017` Win32 套件 |
| 编译器 | Visual Studio 2017，MSVC v141，x86 |
| C++ 标准 | C++17 |
| 构建系统 | CMake 3.19 或更高版本 |
| UI 技术 | Qt Widgets + `.ui` + QSS |
| 数据库 | SQLite，通过 Qt Sql 使用 |
| 相机 SDK | IDS Peak `x86_32` SDK |
| RFID SDK | 厂商 `lib_win32_debug` / `lib_win32_release` 库及 x86 runtime |

本项目统一选择 Qt 5.14.2 + MSVC 2017 x86，与现有 RFID demo 的运行环境保持一致。依据如下：

- 现有 RFID runtime 只提供 x86 `rfidlib_reader.dll`、协议 DLL 与 `Drivers`，不能被 x64 进程加载。
- RFID demo 已在 Qt 5.14.2 + MSVC2017 32 位环境运行，复用该基线可直接进行真实 RFID 联调。
- IDS Peak 安装包同时提供 `x86_32` SDK，可与 x86 Qt 主程序统一进程位数。
- 当前交付优先保证 RFID、CCD、SQLite 在同一 x86 进程中可部署和验证；后续取得完整 x64 RFID runtime 后，可独立评估回迁 Qt 6/x64。

### 5.2 Qt 模块

- `Qt5::Core`
- `Qt5::Gui`
- `Qt5::Widgets`
- `Qt5::Sql`
- `Qt5::Test`：单元测试目标使用

Qt 5 原生提供 `QTextCodec`，用于保持 RFID 的 GBK 编解码行为。迁移后必须运行已有编解码测试，确保标签字节级结果完全一致。

### 5.3 构建约束

- 新工程使用 CMake，不继续使用 qmake `.pro` 文件。
- 主程序、Qt、IDS Peak 和 RFID SDK 必须全部为 x86，禁止混合 32/64 位库。
- Debug 构建链接 RFID Win32 Debug 库，Release 构建链接 RFID Win32 Release 库。
- CMake 通过 `CMAKE_PREFIX_PATH` 指向 Qt 5.14.2 `msvc2017` 套件。
- 开发开始前建立最小依赖探针程序，分别验证 Qt Widgets、SQLite、RFID SDK 和 IDS Peak SDK 在 Debug/Release 下能够链接、启动和卸载。
- 依赖探针未通过前可以继续开发 Mock UI，但不得宣称真实设备技术栈已验证完成。
- SDK 根目录通过 CMake cache 变量或 `CMakePresets.json` 配置，不在源码中硬编码旧 demo 的绝对路径。
- 建议变量为 `IDS_PEAK_ROOT`、`RFID_SDK_ROOT` 和 `RFID_RUNTIME_ROOT`；后者指向包含 `rfidlib_reader.dll` 与 `Drivers` 的 x86 runtime 根目录。
- CMake 配置阶段必须检查关键头文件和库；缺失时给出明确错误，不允许到链接阶段才失败。

## 6. 总体架构

```text
Qt Widgets Pages
    | signals / view models
    v
ApplicationController
    |-- ChamberSessionService
    |-- CaptureWorkflowService
    |
    +--> RfidDeviceService ------> RFID vendor SDK
    +--> CameraDeviceService ----> IDS Peak SDK
    +--> Repository -------------> SQLite + image files
```

### 6.1 分层职责

#### UI 层

- 展示舱室、标签数据、实时帧、16 孔状态和播放状态。
- 接收用户操作并调用应用服务。
- 不直接调用 RFID/相机 SDK。
- 不直接拼接 SQL、文件路径或 RFID payload。

#### 应用层

- 管理当前舱室选择和页面联动。
- 完成 RFID 结果到舱室会话的绑定。
- 管理采集轮次、当前孔位、拍照成功后的自动推进和重拍。
- 管理播放定时器和历史帧索引。

#### 设备层

- `RfidDeviceService` 封装初始化、稳定盘点、标签连接、读取、取消和关闭。
- `CameraDeviceService` 封装相机发现、连接、取流、参数设置、抓拍和关闭。
- 设备调用不得运行在 UI 线程。

#### 存储层

- SQLite 保存结构化数据、舱室绑定、采集轮次和图片索引。
- 图片文件保存无损 PNG。
- 首版不把完整 PNG 同时写入 SQLite BLOB，避免重复占用磁盘空间。

## 7. 推荐目录结构

```text
401_-demo/
|-- CMakeLists.txt
|-- CMakePresets.json
|-- cmake/
|   `-- FindRfidSdk.cmake
|-- resources/
|   |-- resources.qrc
|   |-- icons/
|   `-- styles/
|       `-- default.qss
|-- src/
|   |-- main.cpp
|   |-- app/
|   |   |-- applicationcontroller.*
|   |   |-- chambersessionservice.*
|   |   `-- captureworkflowservice.*
|   |-- domain/
|   |   |-- chambermodel.*
|   |   |-- capturetypes.*
|   |   `-- tagprofile.*
|   |-- devices/
|   |   |-- camera/
|   |   |   |-- cameradeviceservice.*
|   |   |   `-- cameraworker.*
|   |   `-- rfid/
|   |       |-- rfiddeviceservice.*
|   |       |-- tagpayloadcodec.*
|   |       `-- vendoradapter.*
|   |-- storage/
|   |   |-- database.*
|   |   |-- repository.*
|   |   `-- imagefilestore.*
|   `-- ui/
|       |-- mainwindow.*
|       |-- pages/
|       |   |-- homepage.*
|       |   |-- dishdetailpage.*
|       |   `-- welldetailpage.*
|       `-- widgets/
|           |-- chambercard.*
|           |-- wellgrid.*
|           `-- playbackbar.*
`-- tests/
    |-- unit/
    `-- integration/
```

## 8. 现有 demo 复用策略

### 8.1 RFID demo

优先迁移并保留已有职责边界：

- `TagPayload` 和 RFID 错误类型。
- `TagPayloadCodec` 及其字节级测试。
- `RfidTagService`。
- `RfidRetryPolicy` 和 `RfidOperationRunner`。
- `RfidBusinessWorker` 的异步工作方式。
- `RfidDeviceAdapter` 和必要的 `CAEDevice_HF` 底层调用。
- 日志脱敏规则。

不迁移 RFID demo 的调试型 `MainWindow` 页面。设备初始化参数应从统一配置读取，业务结果通过信号返回应用层。

### 8.2 CCD demo

可迁移：

- IDS Peak 初始化、设备打开、DataStream 和缓冲区管理。
- `CameraThread` 中的取流流程和约 30 FPS UI 渲染限流。
- `QImage` 深拷贝后跨线程发送的做法。
- 曝光、增益和单帧抓拍能力。
- 图片播放的定时器与进度控制思路。

需要重构：

- 相机和存储逻辑从 CCD demo 的 `MainWindow` 中移出。
- `CameraThread` 只负责取流，不直接决定业务文件名、孔号或数据库记录。
- 取消原 demo 中图片文件与 SQLite BLOB 双份存储。
- 原“持续保存 N 张”不是 16 孔采集，首版页面不直接暴露为 16 孔功能。
- 原会话数据库升级为标签、舱室、采集轮次和孔位模型。

## 9. 页面规格

### 9.1 首页：四舱总览

#### 页面内容

- 1-4 号舱室卡片，固定 2x2 布局。
- 当前选中舱室有明确边框和选中状态。
- 卡片展示：舱室号、简化状态、培养皿序号、女方姓名、病历号、发育天数和 16 孔缩略图。
- 标签未识别时只显示舱室号和状态。
- 全局设备状态：RFID、CCD、数据库。
- 操作：识别芯片、开启/停止预览、进入皿详情、清除舱室绑定。

#### 简化状态

首版只使用与联动 demo 有关的状态：

```text
未绑定 -> 识别中 -> 已识别 -> 预览中 -> 采集中
                   \-> RFID异常
                   \-> 相机异常
```

这些状态不是完整 PRD 的舱门与拍照调度状态机。

#### 交互规则

- 未选择舱室时禁止发起识别，并提示先选择舱室。
- RFID 识别期间禁止重复识别和切换当前操作舱室。
- 识别成功后立即更新卡片，并在数据库保存 UID 与舱室绑定。
- 同一 UID 已绑定其他舱室时不自动重复绑定，提示用户先清除旧绑定或取消操作。
- 当前舱室已有不同 UID 时，替换前必须二次确认；替换只改变舱室当前绑定，不删除旧标签历史图片。
- 只有已成功识别的舱室可以进入皿详情和创建采集轮次。

### 9.2 皿详情：16 孔预览与采集

#### 页面区域

- 左侧舱室选择：只允许切换到已绑定标签的舱室。
- 患者/培养皿信息：展示 RFID 字段、本地男方姓名和识别时间。
- 16 孔固定 4x4 网格，每个单元尺寸稳定，不因状态文字改变布局。
- 实时预览区域。
- 采集工具栏：新建轮次、拍照、重拍、结束轮次、曝光、增益。
- 历史播放栏：上一轮、播放/暂停、下一轮、倍速、进度条和时间。

#### 孔位状态

| 状态 | 含义 |
| --- | --- |
| 未完成 | 当前轮次没有有效图片 |
| 当前 | 下一次抓拍将写入该孔 |
| 已完成 | 当前轮次已有有效图片 |
| 需重拍 | 图片保存失败或用户主动标记 |

正在拍照时锁定孔位选择，保存成功或失败后恢复。重拍成功后用新记录替换当前轮次的有效图片；旧文件按替换记录保留或进入可清理状态，不直接无提示覆盖。

#### 采集轮次

- 每次“新建轮次”生成一个递增的 `round_no`。
- 同一标签同一时间只允许有一个进行中的轮次。
- 首个当前孔为本轮最小未完成孔号。
- 抓拍成功后自动选择下一个未完成孔。
- 16 孔全部完成时自动标记轮次完成，并提示完成，不自动开始下一轮。
- 允许结束不完整轮次；历史查看时未拍孔显示占位，不使用其他轮次图片冒充。
- 离开页面不停止相机服务，但必须暂停历史播放；是否继续实时预览由用户当前开关决定。

#### 历史播放语义

皿详情中的“视频”是历史图片序列播放，不生成 MP4：

- 时间轴的一个位置对应一个采集轮次。
- 切换轮次时，16 个孔同时显示该轮次各自的图片。
- 某轮次未拍的孔显示明确占位。
- 支持 `1x`、`2x`、`3x`、`4x`，默认 `1x`。
- 拖动进度条后立即显示目标轮次，并保持暂停；用户再次点击播放后继续。

点击任一孔进入该孔详情。

### 9.3 孔详情：单孔历史播放

#### 页面内容

- 舱室号、孔号、培养皿序号、患者信息。
- 1-16 孔位选择器。
- 单孔大图。
- 单层标识。
- 上一帧、播放/暂停、下一帧、倍速、进度条和采集时间。
- 返回皿详情。

#### 播放规则

- 帧序列为当前标签、当前孔位所有有效历史图片，按拍摄时间升序排列。
- 切换孔位后暂停播放并显示该孔最新一帧；没有图片时显示空状态。
- 播放到最后一帧后停止并停留在最后一帧。
- 首版不提供可用的焦平层上下按钮。

## 10. 业务流程

### 10.1 RFID 识别与绑定

```text
用户选择舱室
  -> 检查 RFID 设备可用
  -> 进入识别中状态
  -> 1 秒稳定盘点并确认唯一标签
  -> 连接标签并读取 payload
  -> 校验并解析字段
  -> 检查 UID 是否绑定其他舱室
  -> upsert 标签资料
  -> 写入当前舱室绑定
  -> 加载本地补充字段
  -> 首页卡片更新为已识别
```

读取失败不得清除该舱室上一次已确认绑定，页面显示本次错误并允许重试。

### 10.2 实时预览

```text
用户点击开启预览
  -> 检查当前舱室已绑定
  -> 连接相机（尚未连接时）
  -> 启动 DataStream
  -> 工作线程连续取帧
  -> UI 最多约 30 FPS 渲染最新帧
```

用于预览的丢帧是允许的；抓拍和保存结果不得因 UI 渲染限流丢失。

### 10.3 单孔抓拍

```text
检查当前轮次和当前孔
  -> 锁定拍照按钮及孔位切换
  -> 从相机取得一张完整有效帧
  -> 生成目标路径并写入临时文件
  -> 原子重命名为最终 PNG
  -> SQLite 事务写入图片元数据
  -> 更新网格缩略图
  -> 自动切换到下一个未完成孔
```

文件写入或数据库事务任一失败时，该孔不得标记为完成。界面保留当前孔，允许重试。

## 11. 数据与存储

### 11.1 SQLite 表

#### `tag_profile`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `uid` | TEXT PRIMARY KEY | 标准化后的 RFID UID |
| `format_version` | INTEGER | 标签格式版本 |
| `dish_number` | INTEGER | 标签内培养皿序号 |
| `insemination_time` | TEXT | ISO 8601 |
| `female_name` | TEXT | 标签字段 |
| `medical_record_no` | TEXT | 标签字段 |
| `male_name` | TEXT NULL | 本地补录 |
| `first_seen_at` | TEXT | 首次识别时间 |
| `last_seen_at` | TEXT | 最近识别时间 |

#### `chamber_assignment`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chamber_no` | INTEGER PRIMARY KEY | 1-4 |
| `tag_uid` | TEXT NULL | 当前绑定 UID |
| `identified_at` | TEXT NULL | 本次绑定时间 |
| `updated_at` | TEXT | 更新时间 |

`tag_uid` 非空时必须唯一，保证一个标签不会同时绑定两个舱室。

#### `capture_round`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | INTEGER PRIMARY KEY | 轮次 ID |
| `tag_uid` | TEXT | 所属标签 |
| `chamber_no` | INTEGER | 拍摄时舱室 |
| `round_no` | INTEGER | 标签维度递增轮次 |
| `status` | TEXT | `in_progress`、`completed`、`partial`、`cancelled` |
| `started_at` | TEXT | 开始时间 |
| `finished_at` | TEXT NULL | 结束时间 |

唯一约束：`(tag_uid, round_no)`。

#### `capture_image`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | INTEGER PRIMARY KEY | 图片 ID |
| `round_id` | INTEGER | 所属轮次 |
| `tag_uid` | TEXT | 冗余保存，便于查询和校验 |
| `well_no` | INTEGER | 1-16 |
| `focal_layer` | INTEGER | 首版固定 0 |
| `file_path` | TEXT | 相对数据根目录的路径 |
| `captured_at` | TEXT | ISO 8601，含毫秒 |
| `width` | INTEGER | 像素宽度 |
| `height` | INTEGER | 像素高度 |
| `exposure_us` | REAL | 拍照时曝光 |
| `gain_db` | REAL | 拍照时增益 |
| `is_active` | INTEGER | 当前轮次/孔/层的有效版本 |
| `replaced_image_id` | INTEGER NULL | 重拍替换的旧图片 |

有效图片唯一约束通过事务保证：同一 `(round_id, well_no, focal_layer)` 只有一条 `is_active = 1`。

### 11.2 图片目录

建议数据根目录为应用可写数据目录，而不是安装目录：

```text
data/images/<uid>/<yyyyMMdd>/round_<NNNN>/well_<NN>/
    <timestamp>_layer_00.png
```

- UID 用于路径前必须移除非十六进制或不安全字符。
- 数据库保存相对路径，数据根目录变化时无需批量修改记录。
- 最终文件写入采用临时文件加原子重命名，避免崩溃留下被当作成功图片的半文件。
- 程序启动时检查数据库中的活动图片是否存在；缺失文件标记为不可用并记录错误。

## 12. 线程与生命周期

### 12.1 线程规则

- UI 线程只负责控件、页面导航和轻量状态更新。
- RFID 初始化、盘点、连接和读取在 RFID 工作线程串行执行。
- 相机阻塞取帧在相机工作线程执行。
- PNG 编码、文件保存和数据库写入不得阻塞实时预览；使用独立保存任务串行处理。
- 每个设备只允许一个明确所有者，禁止多个页面直接持有 SDK 句柄。
- 跨线程传递 `QImage` 前必须确保像素内存独立有效。

### 12.2 关闭顺序

```text
禁止新操作
  -> 暂停播放
  -> 等待或取消保存任务
  -> 停止相机采集并回收缓冲区
  -> 取消 RFID 任务并断开标签
  -> 关闭 RFID 读写器
  -> 关闭各线程数据库连接
  -> 退出程序
```

应用不得在线程仍使用读写器、标签或 DataStream 句柄时释放设备对象。

## 13. 错误处理

| 场景 | 页面行为 |
| --- | --- |
| RFID 未连接 | 保留舱室原数据，提示初始化失败并允许重试 |
| 无标签或标签不稳定 | 识别失败，恢复可操作状态 |
| 多标签 | 不自动选取，提示只保留一个标签 |
| 标签 payload 非法 | 不绑定舱室，显示“标签数据异常” |
| UID 已在其他舱室 | 阻止重复绑定并指出已有舱室 |
| 相机断开 | 停止预览和拍照，已保存图片不受影响 |
| 抓帧超时 | 当前孔保持未完成，允许重试 |
| 图片写入失败 | 不写成功元数据，不自动前进孔位 |
| 数据库写入失败 | 当前图片不标记有效，提示存储错误 |
| 历史文件缺失 | 显示缺失占位，跳过播放但不崩溃 |

日志默认不得把完整姓名、病历号或 payload HEX 写入普通本地日志。界面可以显示当前识别结果以便操作人员核对。

## 14. UI 与主题边界

- 页面结构、控件对象名和信号接口在首版确定。
- 颜色、字体、边框、背景、图标和状态动画通过 QSS 与资源文件实现。
- 业务代码禁止根据具体颜色判断状态。
- 图标由资源别名引用，不在业务逻辑中写磁盘绝对路径。
- 后续主题包可以替换 `resources/styles` 和 `resources/icons`，无需修改设备层、应用层或数据库。
- 当前三张 PRD 截图只作为布局参考，不作为最终位图背景嵌入页面。

## 15. 测试要求

### 15.1 单元测试

- RFID 固定 payload 编码结果与原 demo 一致。
- GBK 姓名、BCD 时间、动态病历号长度和非法 payload。
- UID 标准化和同 UID 唯一舱室绑定。
- 发育天数计算边界。
- 新建轮次、自动选择下一个未完成孔、跳孔、补拍和重拍。
- 16 孔完成判断及不完整轮次状态。
- 图片路径生成与非法字符清理。
- 播放索引、倍速、拖动和切换孔位。

### 15.2 数据库集成测试

- 首次启动建库和重复启动迁移幂等。
- RFID 重识别更新标签字段且保留本地男方姓名。
- 一个 UID 不能同时绑定两个舱室。
- 图片文件成功但事务失败时不出现有效图片记录。
- 重拍后只有新图片为活动版本。
- 重启后恢复舱室绑定、历史轮次和 16 孔缩略图。

### 15.3 真机验收

- Qt 5.14.2/MSVC2017 x86 程序能加载 RFID Win32 runtime 和 IDS Peak x86_32 SDK。
- RFID 连续执行 20 次识别，UI 不冻结，结果可正确绑定所选舱室。
- CCD 连续预览 10 分钟，页面可正常切换，应用无崩溃。
- 依次拍摄 16 孔时，每次保存成功后自动前进，允许任意孔重拍。
- 关闭设备、拔插设备和退出程序时无句柄占用导致的崩溃。

## 16. 首版验收场景

1. 启动应用后首页显示 4 个未绑定舱室，数据库和设备状态可见。
2. 选择 2 号舱，执行 RFID 识别，标签字段展示在 2 号舱卡片。
3. 补录男方姓名，重启应用并再次识别同一 UID，补录信息仍能回显。
4. 启动 CCD 预览，画面持续更新，切换页面不阻塞 UI。
5. 进入 2 号舱皿详情，新建轮次，当前孔默认为 1 号孔。
6. 抓拍 1 号孔成功后自动选中 2 号孔；手动点击 5 号孔后可跳拍。
7. 重拍 1 号孔后网格显示新缩略图，历史记录不错误增加一个轮次。
8. 完成 16 个孔后轮次自动完成，首页卡片显示最新 16 孔缩略图。
9. 新建第二轮并拍摄后，皿详情可以按轮次播放 16 孔图像变化。
10. 点击任意孔进入孔详情，可以播放该孔跨轮次的历史图片。
11. RFID、相机或保存失败时页面给出可理解的错误，原有有效数据不丢失。

以上场景全部通过，视为 RFID + CCD 页面联动首版完成。

## 17. 分阶段实施计划

开发采用“先确认界面和联动，再替换真实设备后端，最后精修主题”的顺序。第一阶段产物不是一次性效果图，而是后续真实 RFID/CCD 功能继续使用的正式页面代码。

### 17.1 阶段 0：工程基线与依赖探针

目标：尽早暴露编译器、位数和厂商库兼容问题，同时不阻塞 Mock UI 开发。

- 建立 Qt 5.14.2 + CMake + MSVC 2017 x86 工程。
- 配置统一的 Debug/Release CMake Preset。
- 建立最小依赖探针，验证 Qt Widgets、SQLite、RFID Win32 库和 IDS Peak x86_32 库。
- 固定设备服务接口和 Mock/Real 后端切换方式。

完成标准：主工程可以启动；Qt 和 SQLite 验证通过；RFID、IDS 的链接/加载结果有明确记录。真实设备探针失败时允许进入阶段 1，但必须保留为阶段 2/3 的阻塞项。

### 17.2 阶段 1：正式 UI 骨架与 Mock 全流程

目标：先交付可运行、可评审的三个页面，并确认主要交互，不等待真实设备接入。

- 实现首页四舱卡片、选舱和设备状态区域。
- 实现皿详情的患者信息、实时预览占位、16 孔网格和采集工具栏。
- 实现孔详情的单孔大图、孔位切换和历史播放栏。
- 完成首页、皿详情和孔详情的正式页面导航及返回关系。
- 使用 `MockRfidService` 模拟识别成功、无标签、标签异常和重复 UID。
- 使用 `MockCameraService` 产生稳定变化的测试帧和模拟抓拍结果。
- 跑通“选舱 -> 模拟识别 -> 进入皿详情 -> 新建轮次 -> 拍照自动切孔 -> 重拍 -> 单孔历史播放”。
- 使用临时内存数据驱动页面；阶段 1 不要求先完成最终 SQLite 表。

完成标准：上述主流程可以连续操作；切页和播放不卡 UI；16 孔状态、自动切孔、跳孔和重拍行为符合第 9 节；页面不依赖真实 SDK；后续替换服务实现时不改页面结构。

### 17.3 阶段 2：SQLite 与真实 RFID 接入

目标：用真实标签数据替换 Mock RFID，并让舱室绑定和补录字段可恢复。

- 建立第 11 节 SQLite 模型和仓储接口。
- 迁移 `TagPayloadCodec`、现有单元测试、重试策略和业务工作线程。
- 将 RFID qmake/32 位工程配置迁移为 CMake/x86。
- 接入真实稳定盘点、标签读取、解析和取消流程。
- 将 Mock RFID 结果替换为真实服务结果。
- 完成 UID 唯一舱室绑定、男方姓名本地补录和重启回显。

完成标准：真实 RFID 连续识别并正确更新所选舱室；异常不冻结 UI；重启后绑定和本地补充信息可恢复；Mock RFID 仍可通过配置启用。

### 17.4 阶段 3：真实 CCD 与图片存储接入

目标：用 IDS 相机帧替换 Mock 画面，完成真实抓拍、保存和回放。

- 迁移 IDS Peak 初始化、连接、取流、参数和缓冲区管理。
- 将 CCD demo 的相机逻辑从旧 `MainWindow` 和旧数据库模型中解耦。
- 接入约 30 FPS 实时渲染和真实单帧抓拍。
- 完成 PNG 文件、图片元数据事务和相对路径存储。
- 真实抓拍成功后驱动当前孔完成及自动切孔；失败时保持当前孔。
- 完成皿详情轮次播放和孔详情跨轮次播放。

完成标准：真实相机连续预览稳定；16 孔半自动采集、跳孔和重拍可靠；历史图片重启后可查询播放；Mock Camera 仍可用于无硬件演示。

  ### 17.5 阶段 4：联调、异常恢复与稳定性

- 同时连接 RFID 和 CCD，执行完整首版验收场景。
- 验证拔插设备、超时、保存失败、数据库失败和退出顺序。
- 完成 Debug/Release 真机测试和部署依赖收集。
- 修复内存、线程、句柄和长时间预览问题。

  完成标准：第 15、16 节全部通过，形成可演示的 RFID + CCD 联动版本。

  阶段 4 的执行记录要求：

  - 正常联调必须使用 `TLS401_DEVICE_BACKEND=real`，并记录 RFID 识别、CCD 预览、16 孔采集和历史播放结果。
  - 无硬件时可以先完成 Mock 异常回归，但不得将 Mock 结果标记为真机验收通过。
  - RFID 或 CCD 进入 `Error` 状态时，页面必须显示设备原因；抓拍失败、PNG 写入失败和 SQLite 写入失败不得静默丢失。
  - 关闭窗口时先停止预览、等待采集线程退出，再关闭 RFID/CCD SDK 和 SQLite；重复启动、拔插和退出不得导致句柄占用或崩溃。
  - Debug 与 Release 均需执行依赖探针，并收集 Qt、RFID Win32 runtime、IDS Peak x86_32 runtime 和 `platforms` 目录。

### 17.6 阶段 5：主题资源与视觉定稿

- 接入后续提供的 QSS、字体、图标和图片资源。
- 调整尺寸、间距、状态色和动效，不改变设备及业务接口。
- 在目标分辨率和缩放比例下进行最终页面检查。

完成标准：三个页面视觉统一，无文字裁切、控件重叠或状态表达不清；主题替换未破坏前述功能验收。
